# Проксі для api.telegram.org (потрібен, якщо пряме з'єднання блокується).
# Розкоментуй ОДНЕ з рядків — видали '#' на початку:
# socks5://1.2.3.4:1080
# socks5://user:pass@1.2.3.4:1080
# http://1.2.3.4:3128
# Якщо проксі порожній — бот ходить напряму (наприклад, через VPN на ПК).
